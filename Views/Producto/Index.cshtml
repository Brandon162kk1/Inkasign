@model IEnumerable<Inkasign.Models.Producto>

@{
    ViewData["Title"] = "Prendas de Inkasign Outlet";
}

<center><h1>@ViewData["Title"]</h1></center>
<a class="btn btn-primary" asp-action="Create">Aregar Nuevo Producto</a>
<table class="table">
    <thead>
        <tr>
             <th>
                Imagen    
            </th>
            <th> 
                Nombre
            </th>
            <th>
                Descripción
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
               Estado
            </th>
            <th>
                Opciones
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <div class="card-body">
                  <div class="card-img-actions">
                    <center><img class="img-responsive" width="100" height="100"
                       src="/images/@item.ImageName" alt="@item.ImageName"  /></center>
                  </div>
                </div>   
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                S/. @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Detalles</a>
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
